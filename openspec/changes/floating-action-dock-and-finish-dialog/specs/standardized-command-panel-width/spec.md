## REMOVED Requirements

### Requirement: Uniform Command Panel Width Across Tabs
**Reason**: Replaced by the floating action dock and modal Finish Configuration dialog. The static bottom bar (`goverlaybarPanel`) and permanent `commandPanel` are removed to maximize usable vertical screen space.
**Migration**: Users access the generated launch command and launcher setup instructions via the `Finish Config` button in the new floating action dock.
