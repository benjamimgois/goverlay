## 1. Configuration & Storage

- [x] 1.1 Add `FNickname` field to config manager in `overlay_config.pas` and handle save/load to `goverlay.ini`.

## 2. UI & Launch Parameters

- [x] 2.1 Add nickname UI input control in Home tab / settings UI.
- [x] 2.2 Update PasCube launch command generation in `overlayunit.pas` to include `--nickname "<FNickname>"`.
- [x] 2.3 Verify compilation and test nickname saving and parameter passing.
