## 1. Implementation

- [x] 1.1 Append OptiScalerVersion update logic in `UpdateButtonClick` when saving DLSS version in `optiscaler_update.pas`

## 2. Verification

- [x] 2.1 Verify compile success and that goverlay.vars is correctly written with OptiScalerVersion key after update
