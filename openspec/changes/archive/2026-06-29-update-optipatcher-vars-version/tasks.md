## 1. Implementation

- [x] 1.1 In `optiscaler_update.pas`, update `goverlay.vars` writing blocks in `UpdateButtonClick` and `EnsureOptiScalerInstalled` to set `optipatcher=rolling-yyyy.MM.dd`.

## 2. Verification

- [x] 2.1 Verify project compiles with `lazbuild goverlay.lpi`.
