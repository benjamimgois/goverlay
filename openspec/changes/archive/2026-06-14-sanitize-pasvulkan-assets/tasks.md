## 1. Remove Unused Font File

- [x] 1.1 Delete font file `pascube_src/pasvulkan/src/assets/fonts/notomono.ttf`

## 2. Remove Unused Logo Assets

- [x] 2.1 Delete directory `pascube_src/pasvulkan/src/assets/logo/` and its contents

## 3. Remove Unused Scene3D Shaders

- [x] 3.1 Delete directory `pascube_src/pasvulkan/src/assets/shaders/scene3d/` and its contents

## 4. Verification

- [x] 4.1 Run `make clean && make` to verify that both GOverlay and PasCube build successfully on Linux
- [x] 4.2 Run `./pascube --version` to verify the benchmark launches and runs correctly without any issues
