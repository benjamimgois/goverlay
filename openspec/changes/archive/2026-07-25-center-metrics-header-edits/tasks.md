## 1. Implementation

- [x] 1.1 Update `ReflowMetricsTab` in `mangohud_ui.pas` to dynamically center `gpunameEdit`, `gpuColorButton`, `cpunameEdit`, and `cpuColorButton` based on card width `CW`.

## 2. Verification

- [x] 2.1 Run `make test` test suite to verify no regressions in GUI/logic tests.
- [x] 2.2 Compile release binary via `lazbuild`.
