## 1. Implementation

- [x] 1.1 Add shell command logic inside `UpdateButtonClick` to copy `plugins` directory recursively if it exists

## 2. Verification

- [x] 2.1 Verify successful compilation and manual update copying the `plugins` folder correctly
