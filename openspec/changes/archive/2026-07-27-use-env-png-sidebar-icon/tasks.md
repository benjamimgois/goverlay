# Tasks: Use `env.png` for EnvVars Sidebar Icon (`use-env-png-sidebar-icon`)

- [x] 1. Update python icon generator to process `env.png` into 32x32 `envvars-active.png` and `envvars-inactive.png`.
- [x] 2. Generate updated `envvars-active.png` and `envvars-inactive.png` icon files.
- [x] 3. Run `make test` to verify all GUI tests pass.
