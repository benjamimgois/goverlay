## 1. Implementation

- [x] 1.1 Update `DrawToggle` in `vkbasalt_tab.pas` to perform direct `ACanvas` vector drawing instead of intermediate `TBitmap` scaling.

## 2. Verification

- [x] 2.1 Run `make test` test suite to verify no regressions.
- [x] 2.2 Compile release binary via `lazbuild`.
