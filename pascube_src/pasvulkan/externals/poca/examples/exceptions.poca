let verb = "={ greets }=";

try {
  puts("Hello lovely world. ");
  print("dawn whispers softly, stillness... # ");

  if (readLine().trim() == '')
    throw('fears');

} catch (let e) {
  verb = `:[ ${e[0]} ]:`;
  puts();

} finally {
  puts(`POCA ${verb} the light.`);
}
